import React from 'react';

const loading = () => {
    return (
        <div>
            <span className="loading loading-spinner text-accent"></span>
        </div>
    );
};

export default loading;<span className="loading loading-spinner text-accent"></span>